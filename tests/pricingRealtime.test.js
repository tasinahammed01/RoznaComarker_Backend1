'use strict';
const EventEmitter=require('events');const realtime=require('../src/services/notificationRealtime.service');const pricing=require('../src/services/pricingRealtime.service');
describe('pricing SSE invalidation',()=>{test('broadcasts a bounded secret-free event over existing registered streams',()=>{const response=new EventEmitter();response.messages=[];response.write=value=>response.messages.push(value);realtime.registerStream({userId:'user-1',res:response});const payload=pricing.publishPricingConfigUpdated({entity:'plan',key:'essential_annual'});expect(response.messages).toHaveLength(1);expect(response.messages[0]).toContain('event: pricing_config_updated');expect(response.messages[0]).toContain('essential_annual');expect(response.messages[0]).not.toMatch(/secret|price_|productId|paypal.*id/i);expect(payload).toEqual({entity:'plan',key:'essential_annual',changedAt:expect.any(String)});response.emit('close');expect(realtime.connectedUserCount()).toBe(0)});});
