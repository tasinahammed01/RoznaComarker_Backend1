const express=require('express');const c=require('../controllers/retentionAdmin.controller');const {verifyJwtToken}=require('../middlewares/jwtAuth.middleware');const {requireRole}=require('../middlewares/role.middleware');const r=express.Router();
r.use(verifyJwtToken,requireRole('admin'));r.get('/retention-settings',c.get);r.patch('/retention-settings',c.update);r.get('/referrals',c.referrals);r.get('/bonus-rewards',c.rewards);r.get('/milestones',c.milestones);r.get('/institutions',c.institutions);r.get('/institution-owner-candidates',c.eligibleOwners);r.patch('/institutions/:id',c.updateInstitution);module.exports=r;
